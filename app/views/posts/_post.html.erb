<div id="<%= dom_id post %>">
  <p class="text-3xl font-semibold">
    <%= post.title %>
  </p>
  <div class="divider divider-primary"><%= l post.created_at.to_date, format: :long %></div>
  <%= post.content %>
</div>
