<div class="w-full bg-base-100 shadow-md fixed z-50">
  <div class="navbar max-w-4xl flex justify-between mx-auto">
    <div>
      <%= link_to root_path, class: "btn btn-ghost p-0 m-0 h-full" do %>
        <%= image_tag 'romin-on-rails-no-bg.png', class: "w-20 h-20 p-0 m-0" %>
      <% end %>
    </div>
    <div>
      <ul class="menu menu-horizontal px-1 text-lg flex items-center">
        <li><%= link_to "Sobre", about_path %></li>
        <li><%= link_to "Posts", posts_path %></li>
        <li>
          <select data-choose-theme class="appearance-none bg-transparent border-none cursor-pointer focus:outline-none flex items-center justify-center">
            <option value="dracula">🌜</option>
            <option value="autumn">🌞</option>
          </select>
        </li>
        <%#<li>
          <details>
            <summary>Posts</summary>
            <ul class="bg-base-100 rounded-t-none p-2 w-full">
              <li><a>Link 1</a></li>
              <li><a>Link 2</a></li>
            </ul>
          </details>
        </li>%>
        <li class="hidden lg:block">
          <% if admin? %>
            <%= link_to "Logout", logout_path, class: "text-error", data: { turbo_method: :delete } %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</div>